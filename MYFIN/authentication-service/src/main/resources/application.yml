server:
  port: 9002

spring:
  application:
    name: USER-AUTHENTICATION-SERVICE
  jpa:
    database: POSTGRESQL
    show-sql: true
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/user?currentSchema=authentication
    username: postgres
    password: postgres